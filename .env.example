# API Configuration
# URL base del backend API
# En desarrollo local, dejar comentado para usar el proxy de Vite
# En producción, configurar con la URL real del servidor
VITE_API_URL=http://localhost:8080/api
